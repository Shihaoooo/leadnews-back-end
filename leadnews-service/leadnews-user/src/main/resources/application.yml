server:
  port: 8080
spring:
  application:
    name: leadnews-user
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        service: leadnews-user
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        file-extension: yaml